# AI-ERP — תוכנית 3: אפליקציית הניהול (Next.js)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** אפליקציית ניהול בעברית, RTL, על Vercel: דשבורד, חשבוניות, לידים, לקוחות, מוצרים, משימות, וצ'אט עם סוכן המנהל — קוראת מ-Airtable בצד השרת, כותבת דרך WF13, ומגינה על הכל בסיסמה אחת.

**Architecture:** Next.js 16 App Router. Server Components קוראים מ-Airtable REST (PAT ב-env, `server-only`). כל כתיבה היא Server Action שקורא ל-webhook של n8n (`lib/n8n.ts`) ואז `revalidatePath`. אימות: cookie חתום HMAC שנבדק ב-`proxy.ts`. עיצוב לפי כיוון "פנקס פוגש מכשיר": משטח נייר קרם, טקסט אנתרציט, מבטא כחול-דיו יחיד, LED לסטטוסים, IBM Plex Sans Hebrew + Frank Ruhl Libre. בדיקות: Vitest ל-lib, Playwright smoke לזרימה מקצה לקצה.

**Tech Stack:** Next.js 16.3, React 19, TypeScript, Tailwind CSS 4, shadcn/ui (Button, Input, Select, Dialog, Sheet, Table, Badge, Sonner), next/font (Google), zod, Vitest, Playwright, pnpm, Vercel.

**Spec:** `docs/superpowers/specs/2026-09-02-ai-erp-design.md` (סעיפים 7, 10, 11)

## Global Constraints

- עברית, `<html lang="he" dir="rtl">`, ₪ (`Intl.NumberFormat('he-IL', {style:'currency', currency:'ILS'})`), תאריכים `dd/MM/yyyy`.
- שמות שדות Airtable בדיוק לפי `airtable/schema.md`. סטטוסים: Invoices `new|validated|generated|paid|error`; Leads `New|Contacted|Qualified|Dead|Duplicate`; Tasks `open|done`.
- חוזה WF13: `POST /webhook/erp`, header `x-erp-secret`. `{action:"create", table, payload}` → `{ok, record:{id, createdTime, fields}}`; `{action:"update", table, id, payload}` → `{ok, record}` (נוסף ב-Task 3 של תוכנית זו); `{action:"chat", message, sessionId}` → `{ok, reply}`. שגיאה: `{ok:false, error}`.
- webhooks נוספים: `POST /webhook/reindex-products`, `/webhook/reindex-policies`, `/webhook/run-sales` — אותו header.
- סודות רק ב-env בשרת: `AIRTABLE_PAT`, `AIRTABLE_BASE_ID`, `N8N_WEBHOOK_URL` (= `https://goofy-glamour-syrup.ngrok-free.dev/webhook`), `N8N_WEBHOOK_SECRET`, `APP_PASSWORD`, `AUTH_SECRET`. אף אחד מהם לא מגיע ל-client bundle (רק `lib/*.ts` עם `import 'server-only'` קוראים אותם).
- הוק secret-guard: קובץ התבנית הוא `app/env.example` (בלי נקודה). `.env.local` נערך רק על ידי הסטודנט.
- אין מסד נתונים באפליקציה. אין ORM. אין state management library. אין dark mode.
- עיצוב: טוקנים סמנטיים בלבד (`bg-paper`, `text-ink`, `border-rule`), אף `gray-200`/hex גולמי בקומפוננטות. גריד 8px. גופנים דרך `next/font`. `font-variant-numeric: tabular-nums` על כל מספר.
- כל Server Component שקורא נתונים: `export const dynamic = 'force-dynamic'` (הנתונים חיים ב-Airtable, לא רוצים cache של Next).
- TDD: לכל מודול ב-`lib/` בדיקת Vitest לפני המימוש. לכל מסך בדיקת Playwright אחת לפחות (Task 13).
- commit אחרי כל task.

---

## מבנה קבצים

```
app/                                # תיקיית הפרויקט (pnpm)
  env.example
  proxy.ts                          # auth gate (Next 16: proxy, לא middleware)
  vitest.config.ts · playwright.config.ts
  src/
    app/
      layout.tsx                    # html lang=he dir=rtl, fonts, Toaster
      globals.css                   # טוקנים + Tailwind
      (auth)/login/page.tsx · actions.ts
      (app)/layout.tsx              # Sidebar + Header + ChatSheet
      (app)/page.tsx                # דשבורד
      (app)/invoices/page.tsx · actions.ts · new-invoice-dialog.tsx
      (app)/leads/page.tsx · actions.ts · new-lead-dialog.tsx
      (app)/customers/page.tsx · actions.ts · new-customer-dialog.tsx
      (app)/products/page.tsx · actions.ts · new-product-dialog.tsx
      (app)/tasks/page.tsx · actions.ts
      (app)/chat/actions.ts
      loading.tsx · error.tsx · not-found.tsx
    components/
      ui/*                          # shadcn
      shell/sidebar.tsx · header.tsx · nav-link.tsx
      status-led.tsx                # נקודת LED + טקסט סטטוס
      ledger-strip.tsx              # רצועת KPI של הדשבורד
      money.tsx                     # <Money value/> עם tabular-nums
      empty-state.tsx
      chat/chat-sheet.tsx · chat-panel.tsx
      forms/submit-button.tsx · field-error.tsx
    lib/
      env.ts                        # קריאת env עם zod, server-only
      format.ts                     # ils(), dateIL(), invoiceNumber helpers
      airtable.ts                   # list<T>(table, {filter, sort, max}), get<T>
      types.ts                      # Invoice, Lead, Customer, Product, Task + statuses
      n8n.ts                        # erpCreate, erpUpdate, erpChat, runWebhook
      auth.ts                       # signSession/verifySession (HMAC, Web Crypto)
      status.ts                     # מיפוי סטטוס → תווית עברית + צבע LED
    tests/                          # Vitest: *.test.ts ליד כל lib
  e2e/smoke.spec.ts                 # Playwright
```

---

### Task 1: Scaffold, tokens, fonts, RTL shell

**Files:** Create `app/` (create-next-app), `app/src/app/globals.css`, `app/src/app/layout.tsx`, `app/env.example`, `app/vitest.config.ts`, `app/src/lib/env.ts`, `app/src/lib/env.test.ts`

**Interfaces:**
- Produces: טוקני CSS (`--paper`, `--paper-2`, `--ink`, `--ink-2`, `--ink-3`, `--rule`, `--rule-strong`, `--inkblue`, `--inkblue-hover`, `--led-green`, `--led-amber`, `--led-red`, `--copper`), Tailwind utilities `bg-paper bg-paper-2 text-ink text-ink-2 text-ink-3 border-rule bg-inkblue text-inkblue`, משתני גופן `--font-plex`, `--font-frank`. `env` object: `{ AIRTABLE_PAT, AIRTABLE_BASE_ID, N8N_WEBHOOK_URL, N8N_WEBHOOK_SECRET, APP_PASSWORD, AUTH_SECRET }`.

- [ ] **Step 1: scaffold**

```bash
cd "/Users/felixkreinovich/פרויקט גמר"
pnpm dlx create-next-app@latest app --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm --no-turbopack --yes
cd app && pnpm add zod server-only && pnpm add -D vitest @vitejs/plugin-react vite-tsconfig-paths @playwright/test
pnpm dlx shadcn@latest init -d
pnpm dlx shadcn@latest add button input label select dialog sheet table badge sonner separator skeleton -y
```
Expected: `app/` עם `src/app/page.tsx`, `components.json`, `src/components/ui/*`.

- [ ] **Step 2: env.example + env.ts test**

`app/env.example`:
```
# cp env.example .env.local  ואז מלא. .env.local לא נכנס ל-git.
AIRTABLE_PAT=
AIRTABLE_BASE_ID=app1jXGnS2j0tCxEM
N8N_WEBHOOK_URL=https://goofy-glamour-syrup.ngrok-free.dev/webhook
N8N_WEBHOOK_SECRET=
APP_PASSWORD=
AUTH_SECRET=            # openssl rand -hex 32
```

`app/src/lib/env.test.ts`:
```ts
import { describe, it, expect, vi } from 'vitest';

describe('env', () => {
  it('throws a readable error listing missing variables', async () => {
    vi.stubEnv('AIRTABLE_PAT', '');
    vi.stubEnv('AIRTABLE_BASE_ID', 'app1');
    vi.stubEnv('N8N_WEBHOOK_URL', 'https://x/webhook');
    vi.stubEnv('N8N_WEBHOOK_SECRET', 's');
    vi.stubEnv('APP_PASSWORD', 'p');
    vi.stubEnv('AUTH_SECRET', 'a'.repeat(32));
    const { readEnv } = await import('./env');
    expect(() => readEnv()).toThrow(/AIRTABLE_PAT/);
  });
  it('strips a trailing slash from N8N_WEBHOOK_URL', async () => {
    vi.stubEnv('AIRTABLE_PAT', 'pat');
    vi.stubEnv('AIRTABLE_BASE_ID', 'app1');
    vi.stubEnv('N8N_WEBHOOK_URL', 'https://x/webhook/');
    vi.stubEnv('N8N_WEBHOOK_SECRET', 's');
    vi.stubEnv('APP_PASSWORD', 'p');
    vi.stubEnv('AUTH_SECRET', 'a'.repeat(32));
    const { readEnv } = await import('./env');
    expect(readEnv().N8N_WEBHOOK_URL).toBe('https://x/webhook');
  });
});
```

`app/vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
import tsconfigPaths from 'vite-tsconfig-paths';
export default defineConfig({
  plugins: [tsconfigPaths()],
  test: { environment: 'node', include: ['src/**/*.test.ts'], unstubEnvs: true },
});
```
ב-`package.json` scripts: `"test": "vitest run"`, `"test:watch": "vitest"`, `"e2e": "playwright test"`.

- [ ] **Step 3: run — expect FAIL** — `pnpm test` → `Cannot find module './env'`.

- [ ] **Step 4: env.ts**

```ts
import 'server-only';
import { z } from 'zod';

const schema = z.object({
  AIRTABLE_PAT: z.string().min(1),
  AIRTABLE_BASE_ID: z.string().regex(/^app[A-Za-z0-9]{14}$/),
  N8N_WEBHOOK_URL: z.string().url().transform((u) => u.replace(/\/+$/, '')),
  N8N_WEBHOOK_SECRET: z.string().min(1),
  APP_PASSWORD: z.string().min(1),
  AUTH_SECRET: z.string().min(32),
});
export type Env = z.infer<typeof schema>;

export function readEnv(): Env {
  const parsed = schema.safeParse(process.env);
  if (!parsed.success) {
    const missing = parsed.error.issues.map((i) => i.path.join('.')).join(', ');
    throw new Error(`Missing/invalid env: ${missing}`);
  }
  return parsed.data;
}
let cached: Env | undefined;
export function env(): Env { return (cached ??= readEnv()); }
```
`server-only` זורק ב-Vitest? לא — הוא זורק רק כשמיובא מ-client bundle. ב-vitest הוא no-op. אם בכל זאת נכשל: `vi.mock('server-only', () => ({}))` בראש הבדיקה.

- [ ] **Step 5: run — expect PASS** — `pnpm test`.

- [ ] **Step 6: globals.css — טוקנים** (מחליף את מה ש-shadcn יצר, שומר את ה-`@import "tailwindcss"` וה-`@theme`):

```css
@import "tailwindcss";
@import "tw-animate-css";

:root {
  /* עולם: פנקס חשבוניות על שולחן עבודה של חנות אלקטרוניקה */
  --paper: #f7f3ea;        /* נייר הפנקס */
  --paper-2: #fdfbf6;      /* דף עליון, כרטיס */
  --paper-3: #efe9dc;      /* שקע: input, שורה מודגשת */
  --ink: #1f2326;          /* אנתרציט של מכשיר */
  --ink-2: #4b5157;        /* טקסט משני */
  --ink-3: #8a9097;        /* מטא, מושבת */
  --rule: rgba(31, 35, 38, 0.10);
  --rule-strong: rgba(31, 35, 38, 0.22);
  --inkblue: #2b4c7e;      /* דיו כחול של העתק */
  --inkblue-hover: #23406b;
  --inkblue-soft: rgba(43, 76, 126, 0.10);
  --led-green: #2f9e5b;
  --led-amber: #d08a1d;
  --led-red: #c4453a;
  --copper: #b87333;
  --radius-sm: 4px; --radius-md: 8px; --radius-lg: 12px;
}

@theme inline {
  --color-paper: var(--paper); --color-paper-2: var(--paper-2); --color-paper-3: var(--paper-3);
  --color-ink: var(--ink); --color-ink-2: var(--ink-2); --color-ink-3: var(--ink-3);
  --color-rule: var(--rule); --color-rule-strong: var(--rule-strong);
  --color-inkblue: var(--inkblue); --color-inkblue-hover: var(--inkblue-hover); --color-inkblue-soft: var(--inkblue-soft);
  --color-led-green: var(--led-green); --color-led-amber: var(--led-amber); --color-led-red: var(--led-red);
  --color-copper: var(--copper);
  --font-sans: var(--font-plex); --font-display: var(--font-frank);
  --radius-sm: var(--radius-sm); --radius-md: var(--radius-md); --radius-lg: var(--radius-lg);
  /* shadcn semantic → העולם שלנו */
  --color-background: var(--paper); --color-foreground: var(--ink);
  --color-card: var(--paper-2); --color-card-foreground: var(--ink);
  --color-popover: var(--paper-2); --color-popover-foreground: var(--ink);
  --color-primary: var(--inkblue); --color-primary-foreground: #fff;
  --color-secondary: var(--paper-3); --color-secondary-foreground: var(--ink);
  --color-muted: var(--paper-3); --color-muted-foreground: var(--ink-2);
  --color-accent: var(--inkblue-soft); --color-accent-foreground: var(--inkblue);
  --color-destructive: var(--led-red);
  --color-border: var(--rule); --color-input: var(--rule-strong); --color-ring: var(--inkblue);
}

html { -webkit-font-smoothing: antialiased; }
body { background: var(--paper); color: var(--ink); font-family: var(--font-plex), system-ui, sans-serif; font-size: 14px; line-height: 1.5; }
h1, h2 { font-family: var(--font-frank), serif; letter-spacing: -0.01em; text-wrap: balance; }
.num { font-variant-numeric: tabular-nums; }
p, td { text-wrap: pretty; }
```

- [ ] **Step 7: layout.tsx**

```tsx
import type { Metadata } from 'next';
import { IBM_Plex_Sans_Hebrew, Frank_Ruhl_Libre } from 'next/font/google';
import { Toaster } from '@/components/ui/sonner';
import './globals.css';

const plex = IBM_Plex_Sans_Hebrew({ subsets: ['hebrew', 'latin'], weight: ['400', '500', '600'], variable: '--font-plex' });
const frank = Frank_Ruhl_Libre({ subsets: ['hebrew', 'latin'], weight: ['500', '700'], variable: '--font-frank' });

export const metadata: Metadata = { title: 'AI-ERP · איי.איי אלקטרוניקה', description: 'מערכת ניהול חכמה' };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="he" dir="rtl" className={`${plex.variable} ${frank.variable}`}>
      <body>{children}<Toaster position="bottom-left" richColors /></body>
    </html>
  );
}
```
מוחקים את התוכן של `src/app/page.tsx` ל-`export default function Page(){ return null }` זמנית (Task 6 מחליף).

- [ ] **Step 8: build + commit**

```bash
pnpm lint && pnpm build && cd .. && git add app && git commit -m "feat(app): scaffold next 16, tokens, fonts, rtl shell, env validation"
```
Expected: build exit 0. (`.gitignore` של הריפו כבר מכסה `node_modules`, `.next`, `.env*`.)

---

### Task 2: lib/format, lib/status, lib/types

**Files:** Create `src/lib/format.ts`, `format.test.ts`, `src/lib/status.ts`, `status.test.ts`, `src/lib/types.ts`

**Interfaces (Produces):**
- `ils(n: number): string` → `"1,180.00 ₪"`; `dateIL(iso: string): string` → `"02/09/2026"`; `monthKey(iso)` → `"2026-09"`.
- `types.ts`: `type Rec<F> = { id: string; createdTime: string; fields: F }`; `Invoice`, `Lead`, `Customer`, `Product`, `Task` = `Rec<...Fields>`; `INVOICE_STATUSES`, `LEAD_STATUSES`, `TASK_STATUSES` as const; `TableName = 'Invoices'|'Leads'|'Customers'|'Products'|'Tasks'`.
- `status.ts`: `statusMeta(table: TableName, status?: string): { label: string; led: 'green'|'amber'|'red'|'off' }`.

- [ ] **Step 1: tests**

```ts
// format.test.ts
import { describe, it, expect } from 'vitest';
import { ils, dateIL, monthKey } from './format';
describe('format', () => {
  it('formats ILS with two decimals and shekel sign', () => {
    expect(ils(1180)).toBe('1,180.00 ₪');
    expect(ils(0)).toBe('0.00 ₪');
  });
  it('formats ISO dates as dd/MM/yyyy in Israel time', () => {
    expect(dateIL('2026-09-02T08:28:29.000Z')).toBe('02/09/2026');
    expect(dateIL('2026-01-01T23:30:00.000Z')).toBe('02/01/2026'); // אחרי חצות בישראל
  });
  it('monthKey', () => { expect(monthKey('2026-09-02T08:28:29.000Z')).toBe('2026-09'); });
});
```
```ts
// status.test.ts
import { describe, it, expect } from 'vitest';
import { statusMeta } from './status';
describe('statusMeta', () => {
  it('maps invoice statuses to hebrew + led', () => {
    expect(statusMeta('Invoices', 'generated')).toEqual({ label: 'הופק PDF', led: 'green' });
    expect(statusMeta('Invoices', 'validated')).toEqual({ label: 'אומת', led: 'amber' });
    expect(statusMeta('Invoices', 'error')).toEqual({ label: 'שגיאה', led: 'red' });
    expect(statusMeta('Invoices', undefined)).toEqual({ label: 'חדש', led: 'amber' });
  });
  it('maps lead statuses', () => {
    expect(statusMeta('Leads', 'Qualified').led).toBe('green');
    expect(statusMeta('Leads', 'Dead').led).toBe('off');
    expect(statusMeta('Leads', 'weird').label).toBe('weird');
  });
});
```

- [ ] **Step 2: run — FAIL** (`pnpm test`).

- [ ] **Step 3: implement**

```ts
// format.ts
const ilsFmt = new Intl.NumberFormat('he-IL', { style: 'currency', currency: 'ILS', minimumFractionDigits: 2 });
export const ils = (n: number) => ilsFmt.format(n).replace('‏', '').replace(/^₪\s?/, '').trim() + ' ₪';
const dateFmt = new Intl.DateTimeFormat('en-GB', { timeZone: 'Asia/Jerusalem', day: '2-digit', month: '2-digit', year: 'numeric' });
export const dateIL = (iso: string) => dateFmt.format(new Date(iso)); // dd/MM/yyyy
export const monthKey = (iso: string) => {
  const p = new Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Jerusalem', year: 'numeric', month: '2-digit' }).formatToParts(new Date(iso));
  return `${p.find((x) => x.type === 'year')!.value}-${p.find((x) => x.type === 'month')!.value}`;
};
```
אם `ils(1180)` יוצא `"‏1,180.00 ₪"` עם סימן RTL — ה-`replace('‏','')` מטפל; אם Intl מוציא `₪1,180.00` — ה-regex מסיר את ה-₪ הקידומי ומוסיף בסוף. הבדיקה קובעת את הפורמט.

```ts
// types.ts
export type Rec<F> = { id: string; createdTime: string; fields: F };
export const INVOICE_STATUSES = ['new', 'validated', 'generated', 'paid', 'error'] as const;
export const LEAD_STATUSES = ['New', 'Contacted', 'Qualified', 'Dead', 'Duplicate'] as const;
export const TASK_STATUSES = ['open', 'done'] as const;
export type InvoiceStatus = (typeof INVOICE_STATUSES)[number];
export type LeadStatus = (typeof LEAD_STATUSES)[number];
export type TaskStatus = (typeof TASK_STATUSES)[number];
export type InvoiceFields = { InvoiceNumber?: string; CustomerId: string; Amount: number; VatAmount?: number; Total?: number; Status?: InvoiceStatus; PdfUrl?: string; Created: string };
export type LeadFields = { Name: string; Email?: string; Company?: string; Status?: LeadStatus; Created: string };
export type CustomerFields = { CustomerId: string; Name: string; Email?: string; Phone?: string };
export type ProductFields = { Name: string; Category?: string; Price?: number; Description?: string; InStock?: boolean };
export type TaskFields = { Title: string; Status?: TaskStatus };
export type Invoice = Rec<InvoiceFields>; export type Lead = Rec<LeadFields>; export type Customer = Rec<CustomerFields>;
export type Product = Rec<ProductFields>; export type Task = Rec<TaskFields>;
export type TableName = 'Invoices' | 'Leads' | 'Customers' | 'Products' | 'Tasks';
```
```ts
// status.ts
import type { TableName } from './types';
type Led = 'green' | 'amber' | 'red' | 'off';
const MAP: Record<TableName, Record<string, { label: string; led: Led }>> = {
  Invoices: { new: { label: 'חדש', led: 'amber' }, validated: { label: 'אומת', led: 'amber' }, generated: { label: 'הופק PDF', led: 'green' }, paid: { label: 'שולם', led: 'green' }, error: { label: 'שגיאה', led: 'red' } },
  Leads: { New: { label: 'חדש', led: 'amber' }, Contacted: { label: 'נשלח מייל', led: 'amber' }, Qualified: { label: 'ענה — מתאים', led: 'green' }, Dead: { label: 'סגור', led: 'off' }, Duplicate: { label: 'כפול', led: 'off' } },
  Tasks: { open: { label: 'פתוחה', led: 'amber' }, done: { label: 'בוצעה', led: 'green' } },
  Customers: {}, Products: {},
};
const DEFAULT: Record<TableName, string> = { Invoices: 'new', Leads: 'New', Tasks: 'open', Customers: '', Products: '' };
export function statusMeta(table: TableName, status?: string) {
  const key = status ?? DEFAULT[table];
  return MAP[table][key] ?? { label: key, led: 'off' as Led };
}
```

- [ ] **Step 4: run — PASS**, **Step 5: commit** `feat(app): format, status and record types`.

---

### Task 3: WF13 — פעולת `update` (צד n8n) + lib/n8n

**Files:** Modify `n8n/workflows/13-api.json`; Create `src/lib/n8n.ts`, `n8n.test.ts`

**Interfaces (Produces):**
- WF13 מקבל `{action:"update", table, id, payload}` → `{ok:true, record}`.
- `erpCreate<F>(table: TableName, payload: Partial<F>): Promise<Rec<F>>`, `erpUpdate<F>(table, id, payload): Promise<Rec<F>>`, `erpChat(message: string, sessionId: string): Promise<string>`, `runWebhook(path: 'reindex-products'|'reindex-policies'|'run-sales'): Promise<unknown>`. כולן זורקות `ErpError(message)` על `ok:false` או HTTP לא 2xx.

- [ ] **Step 1: WF13 — rule שלישי ב-Switch + ענף update** — ב-`13-api.json` מוסיפים rule `{ leftValue: "={{ $json.body.action }}", rightValue: "update", outputKey: "update" }` אחרי `chat`, צומת:

```json
{ "name": "Update Payload", "type": "n8n-nodes-base.set", "typeVersion": 3.4, "position": [520, 340],
  "parameters": { "mode": "raw", "jsonOutput": "={{ Object.assign({ id: $json.body.id }, $json.body.payload) }}", "options": {} } },
{ "name": "Update Record", "type": "n8n-nodes-base.airtable", "typeVersion": 2.2, "position": [780, 340],
  "parameters": { "authentication": "airtableTokenApi", "operation": "update",
    "base": { "__rl": true, "mode": "id", "value": "__AIRTABLE_BASE_ID__" },
    "table": { "__rl": true, "mode": "id", "value": "={{ $('Webhook').item.json.body.table }}" },
    "columns": { "mappingMode": "autoMapInputData", "value": {}, "matchingColumns": ["id"], "schema": [] },
    "options": { "typecast": true } },
  "credentials": { "airtableTokenApi": { "id": "__CRED_AIRTABLE__", "name": "Airtable ERP" } } },
{ "name": "Respond Updated", "type": "n8n-nodes-base.respondToWebhook", "typeVersion": 1.1, "position": [1040, 340],
  "parameters": { "respondWith": "json", "responseBody": "={{ JSON.stringify({ ok: true, record: $json }) }}", "options": {} } }
```
connections: Route output index 2 → Update Payload (הfallback עובר לאינדקס 3 → Respond Bad Request), Update Payload → Update Record → Respond Updated.

```bash
n8n/scripts/import-workflow.sh n8n/workflows/13-api.json --activate
n8n/scripts/api-test.sh '{"action":"update","table":"Tasks","id":"recd14UiYcozlAgDb","payload":{"Status":"done"}}'
n8n/scripts/api-test.sh '{"action":"update","table":"Tasks","id":"recd14UiYcozlAgDb","payload":{"Status":"open"}}'
```
Expected: שני `{"ok":true,"record":{...}} HTTP 200`. אם autoMap עם `matchingColumns:["id"]` נכשל: מחליפים ל-`mappingMode:"defineBelow"` עם `value: {"id":"={{ $json.id }}", ...}` — לא אפשרי דינמית; אז fallback: HTTP Request node `PATCH https://api.airtable.com/v0/__AIRTABLE_BASE_ID__/{{table}}/{{id}}` body `{"fields": payload, "typecast": true}` עם credential airtableTokenApi.

- [ ] **Step 2: n8n.test.ts**

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
vi.mock('server-only', () => ({}));
vi.mock('./env', () => ({ env: () => ({ N8N_WEBHOOK_URL: 'https://n8n.test/webhook', N8N_WEBHOOK_SECRET: 'shh' }) }));
import { erpCreate, erpUpdate, erpChat, runWebhook, ErpError } from './n8n';

const ok = (body: unknown, status = 200) => new Response(JSON.stringify(body), { status, headers: { 'content-type': 'application/json' } });

describe('n8n client', () => {
  beforeEach(() => { vi.restoreAllMocks(); });
  it('erpCreate posts action/table/payload with secret header and returns record', async () => {
    const fetchMock = vi.spyOn(globalThis, 'fetch').mockResolvedValue(ok({ ok: true, record: { id: 'rec1', createdTime: 't', fields: { Title: 'x' } } }));
    const rec = await erpCreate('Tasks', { Title: 'x' });
    expect(rec.id).toBe('rec1');
    const [url, init] = fetchMock.mock.calls[0];
    expect(url).toBe('https://n8n.test/webhook/erp');
    expect((init!.headers as Record<string, string>)['x-erp-secret']).toBe('shh');
    expect(JSON.parse(init!.body as string)).toEqual({ action: 'create', table: 'Tasks', payload: { Title: 'x' } });
  });
  it('erpUpdate sends id', async () => {
    vi.spyOn(globalThis, 'fetch').mockResolvedValue(ok({ ok: true, record: { id: 'rec1', createdTime: 't', fields: { Status: 'done' } } }));
    const rec = await erpUpdate('Tasks', 'rec1', { Status: 'done' });
    expect(rec.fields).toEqual({ Status: 'done' });
  });
  it('erpChat returns reply', async () => {
    vi.spyOn(globalThis, 'fetch').mockResolvedValue(ok({ ok: true, reply: 'שלום' }));
    expect(await erpChat('מה ההכנסות?', 'sess')).toBe('שלום');
  });
  it('throws ErpError on ok:false and on http error', async () => {
    vi.spyOn(globalThis, 'fetch').mockResolvedValueOnce(ok({ ok: false, error: 'unknown action: x' }, 400));
    await expect(erpCreate('Tasks', {})).rejects.toBeInstanceOf(ErpError);
    vi.spyOn(globalThis, 'fetch').mockResolvedValueOnce(new Response('gateway', { status: 502 }));
    await expect(runWebhook('run-sales')).rejects.toThrow(/502/);
  });
});
```

- [ ] **Step 3: run — FAIL**, **Step 4: implement**

```ts
import 'server-only';
import { env } from './env';
import type { Rec, TableName } from './types';

export class ErpError extends Error {}

async function post<T>(path: string, body: unknown): Promise<T> {
  const { N8N_WEBHOOK_URL, N8N_WEBHOOK_SECRET } = env();
  const res = await fetch(`${N8N_WEBHOOK_URL}/${path}`, {
    method: 'POST', cache: 'no-store',
    headers: { 'content-type': 'application/json', 'x-erp-secret': N8N_WEBHOOK_SECRET },
    body: JSON.stringify(body),
  });
  const text = await res.text();
  let data: { ok?: boolean; error?: string } & T;
  try { data = JSON.parse(text); } catch { throw new ErpError(`n8n ${res.status}: ${text.slice(0, 200)}`); }
  if (!res.ok || data.ok === false) throw new ErpError(data.error ?? `n8n ${res.status}`);
  return data;
}
export async function erpCreate<F>(table: TableName, payload: Partial<F>) {
  return (await post<{ record: Rec<F> }>('erp', { action: 'create', table, payload })).record;
}
export async function erpUpdate<F>(table: TableName, id: string, payload: Partial<F>) {
  return (await post<{ record: Rec<F> }>('erp', { action: 'update', table, id, payload })).record;
}
export async function erpChat(message: string, sessionId: string) {
  return (await post<{ reply: string }>('erp', { action: 'chat', message, sessionId })).reply;
}
export type WebhookPath = 'reindex-products' | 'reindex-policies' | 'run-sales';
export const runWebhook = (path: WebhookPath) => post<unknown>(path, {});
```

- [ ] **Step 5: run — PASS**, **Step 6: commit** `feat: WF13 update action; n8n client with tests`.

---

### Task 4: lib/airtable — קריאה בצד השרת

**Files:** Create `src/lib/airtable.ts`, `airtable.test.ts`

**Interfaces (Produces):**
- `list<F>(table: TableName, opts?: { filter?: string; sort?: { field: string; direction?: 'asc'|'desc' }[]; max?: number }): Promise<Rec<F>[]>` — מטפל ב-`offset` (pagination), `cache:'no-store'`.
- `get<F>(table, id): Promise<Rec<F> | null>`.
- `escapeFormula(s: string)` — מצטט מחרוזת לתוך `filterByFormula` (`'` → `\'`).

- [ ] **Step 1: test**

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
vi.mock('server-only', () => ({}));
vi.mock('./env', () => ({ env: () => ({ AIRTABLE_PAT: 'pat', AIRTABLE_BASE_ID: 'appX' }) }));
import { list, get, escapeFormula } from './airtable';
const json = (b: unknown, status = 200) => new Response(JSON.stringify(b), { status, headers: { 'content-type': 'application/json' } });

describe('airtable', () => {
  beforeEach(() => vi.restoreAllMocks());
  it('lists with filter/sort/max and follows offset', async () => {
    const f = vi.spyOn(globalThis, 'fetch')
      .mockResolvedValueOnce(json({ records: [{ id: 'a', createdTime: 't', fields: {} }], offset: 'o1' }))
      .mockResolvedValueOnce(json({ records: [{ id: 'b', createdTime: 't', fields: {} }] }));
    const rows = await list('Invoices', { filter: "{Status}='new'", sort: [{ field: 'Created', direction: 'desc' }], max: 50 });
    expect(rows.map((r) => r.id)).toEqual(['a', 'b']);
    const u1 = new URL(f.mock.calls[0][0] as string);
    expect(u1.pathname).toBe('/v0/appX/Invoices');
    expect(u1.searchParams.get('filterByFormula')).toBe("{Status}='new'");
    expect(u1.searchParams.get('sort[0][field]')).toBe('Created');
    expect(u1.searchParams.get('maxRecords')).toBe('50');
    expect((f.mock.calls[0][1]!.headers as Record<string, string>).Authorization).toBe('Bearer pat');
    expect(new URL(f.mock.calls[1][0] as string).searchParams.get('offset')).toBe('o1');
  });
  it('get returns null on 404 and throws on 5xx', async () => {
    vi.spyOn(globalThis, 'fetch').mockResolvedValueOnce(new Response('nf', { status: 404 }));
    expect(await get('Tasks', 'recX')).toBeNull();
    vi.spyOn(globalThis, 'fetch').mockResolvedValueOnce(new Response('boom', { status: 500 }));
    await expect(list('Tasks')).rejects.toThrow(/500/);
  });
  it('escapeFormula escapes quotes', () => { expect(escapeFormula("O'Neil")).toBe("O\\'Neil"); });
});
```

- [ ] **Step 2: FAIL**, **Step 3: implement**

```ts
import 'server-only';
import { env } from './env';
import type { Rec, TableName } from './types';

type Sort = { field: string; direction?: 'asc' | 'desc' };
export type ListOpts = { filter?: string; sort?: Sort[]; max?: number };

function headers() { return { Authorization: `Bearer ${env().AIRTABLE_PAT}` }; }
function base() { return `https://api.airtable.com/v0/${env().AIRTABLE_BASE_ID}`; }

export const escapeFormula = (s: string) => s.replace(/\\/g, '\\\\').replace(/'/g, "\\'");

export async function list<F>(table: TableName, opts: ListOpts = {}): Promise<Rec<F>[]> {
  const out: Rec<F>[] = [];
  let offset: string | undefined;
  do {
    const url = new URL(`${base()}/${encodeURIComponent(table)}`);
    if (opts.filter) url.searchParams.set('filterByFormula', opts.filter);
    opts.sort?.forEach((s, i) => { url.searchParams.set(`sort[${i}][field]`, s.field); url.searchParams.set(`sort[${i}][direction]`, s.direction ?? 'asc'); });
    if (opts.max) url.searchParams.set('maxRecords', String(opts.max));
    if (offset) url.searchParams.set('offset', offset);
    const res = await fetch(url, { headers: headers(), cache: 'no-store' });
    if (!res.ok) throw new Error(`Airtable ${res.status} on ${table}: ${(await res.text()).slice(0, 200)}`);
    const data = (await res.json()) as { records: Rec<F>[]; offset?: string };
    out.push(...data.records);
    offset = data.offset;
  } while (offset && (!opts.max || out.length < opts.max));
  return opts.max ? out.slice(0, opts.max) : out;
}

export async function get<F>(table: TableName, id: string): Promise<Rec<F> | null> {
  const res = await fetch(`${base()}/${encodeURIComponent(table)}/${id}`, { headers: headers(), cache: 'no-store' });
  if (res.status === 404) return null;
  if (!res.ok) throw new Error(`Airtable ${res.status}`);
  return (await res.json()) as Rec<F>;
}
```

- [ ] **Step 4: PASS**, **Step 5: commit** `feat: airtable server client`.

---

### Task 5: אימות — lib/auth, proxy.ts, login

**Files:** Create `src/lib/auth.ts`, `auth.test.ts`, `app/proxy.ts`, `src/app/(auth)/login/page.tsx`, `src/app/(auth)/login/actions.ts`

**Interfaces (Produces):**
- `signSession(secret: string, ttlSeconds?: number): Promise<string>` — `"<exp>.<hmac-hex>"`; `verifySession(token: string | undefined, secret: string): Promise<boolean>`. Web Crypto בלבד (רץ גם ב-proxy).
- cookie `erp_session` (httpOnly, secure בפרודקשן, sameSite lax, 30 יום). `proxy.ts` מפנה ל-`/login?next=` כל נתיב חוץ מ-`/login`, `/_next`, `/favicon.ico`.
- Server actions: `login(prev, formData)` → `{ error?: string }`, `logout()`.

- [ ] **Step 1: test**

```ts
import { describe, it, expect } from 'vitest';
import { signSession, verifySession } from './auth';
const secret = 's'.repeat(32);
describe('auth session', () => {
  it('round-trips a valid token', async () => { expect(await verifySession(await signSession(secret, 60), secret)).toBe(true); });
  it('rejects tampered, expired and missing tokens', async () => {
    const t = await signSession(secret, 60);
    expect(await verifySession(t.replace(/.$/, (c) => (c === 'a' ? 'b' : 'a')), secret)).toBe(false);
    expect(await verifySession(await signSession(secret, -1), secret)).toBe(false);
    expect(await verifySession(undefined, secret)).toBe(false);
    expect(await verifySession(t, 'x'.repeat(32))).toBe(false);
  });
});
```

- [ ] **Step 2: FAIL**, **Step 3: implement**

```ts
// auth.ts — ללא server-only: proxy.ts משתמש בזה
const enc = new TextEncoder();
async function hmac(secret: string, msg: string) {
  const key = await crypto.subtle.importKey('raw', enc.encode(secret), { name: 'HMAC', hash: 'SHA-256' }, false, ['sign']);
  const sig = await crypto.subtle.sign('HMAC', key, enc.encode(msg));
  return Array.from(new Uint8Array(sig)).map((b) => b.toString(16).padStart(2, '0')).join('');
}
export const SESSION_COOKIE = 'erp_session';
export async function signSession(secret: string, ttlSeconds = 60 * 60 * 24 * 30) {
  const exp = Math.floor(Date.now() / 1000) + ttlSeconds;
  return `${exp}.${await hmac(secret, String(exp))}`;
}
export async function verifySession(token: string | undefined, secret: string) {
  if (!token) return false;
  const [expStr, sig] = token.split('.');
  const exp = Number(expStr);
  if (!exp || !sig || exp < Date.now() / 1000) return false;
  const expected = await hmac(secret, expStr);
  if (expected.length !== sig.length) return false;
  let diff = 0; for (let i = 0; i < sig.length; i++) diff |= sig.charCodeAt(i) ^ expected.charCodeAt(i);
  return diff === 0;
}
```

```ts
// app/proxy.ts  (Next 16)
import { NextResponse, type NextRequest } from 'next/server';
import { SESSION_COOKIE, verifySession } from '@/lib/auth';
export async function proxy(req: NextRequest) {
  const { pathname } = req.nextUrl;
  if (pathname.startsWith('/login') || pathname.startsWith('/_next') || pathname === '/favicon.ico') return NextResponse.next();
  const ok = await verifySession(req.cookies.get(SESSION_COOKIE)?.value, process.env.AUTH_SECRET ?? '');
  if (ok) return NextResponse.next();
  const url = req.nextUrl.clone(); url.pathname = '/login'; url.searchParams.set('next', pathname);
  return NextResponse.redirect(url);
}
export const config = { matcher: ['/((?!api/health).*)'] };
```

```ts
// (auth)/login/actions.ts
'use server';
import { cookies } from 'next/headers';
import { redirect } from 'next/navigation';
import { env } from '@/lib/env';
import { SESSION_COOKIE, signSession } from '@/lib/auth';
export async function login(_prev: { error?: string } | undefined, formData: FormData) {
  const password = String(formData.get('password') ?? '');
  const next = String(formData.get('next') ?? '/');
  if (password !== env().APP_PASSWORD) return { error: 'סיסמה שגויה' };
  (await cookies()).set(SESSION_COOKIE, await signSession(env().AUTH_SECRET), { httpOnly: true, sameSite: 'lax', secure: process.env.NODE_ENV === 'production', path: '/', maxAge: 60 * 60 * 24 * 30 });
  redirect(next.startsWith('/') ? next : '/');
}
export async function logout() { (await cookies()).delete(SESSION_COOKIE); redirect('/login'); }
```

```tsx
// (auth)/login/page.tsx
'use client';
import { useActionState } from 'react';
import { useSearchParams } from 'next/navigation';
import { login } from './actions';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
export default function LoginPage() {
  const [state, action, pending] = useActionState(login, undefined);
  const next = useSearchParams().get('next') ?? '/';
  return (
    <main className="min-h-dvh grid place-items-center p-6">
      <form action={action} className="w-full max-w-sm bg-paper-2 border border-rule rounded-lg p-8 shadow-[0_0_0_1px_rgba(31,35,38,.04),0_1px_2px_-1px_rgba(31,35,38,.06),0_2px_4px_rgba(31,35,38,.04)]">
        <div className="text-xs font-medium tracking-wide text-ink-3">AI-ERP</div>
        <h1 className="text-2xl font-bold mt-1">איי.איי אלקטרוניקה</h1>
        <p className="text-ink-2 mt-1">כניסה למערכת הניהול</p>
        <input type="hidden" name="next" value={next} />
        <div className="mt-6 space-y-2">
          <Label htmlFor="password">סיסמה</Label>
          <Input id="password" name="password" type="password" autoFocus required autoComplete="current-password" />
          {state?.error && <p role="alert" className="text-led-red text-sm">{state.error}</p>}
        </div>
        <Button type="submit" className="mt-6 w-full" disabled={pending}>{pending ? 'בודק…' : 'כניסה'}</Button>
      </form>
    </main>
  );
}
```
`useSearchParams` בקומפוננטת client בתוך page — עוטפים ב-`<Suspense>` ב-page (Next דורש): מפצלים ל-`LoginForm` client + `page.tsx` server שמרנדר `<Suspense><LoginForm/></Suspense>`.

- [ ] **Step 4: PASS + ידני**: `cp env.example .env.local` (הסטודנט ממלא: PAT, secret של n8n, סיסמה, `openssl rand -hex 32`), `pnpm dev` → `http://localhost:3000` מפנה ל-`/login`; סיסמה שגויה → "סיסמה שגויה"; נכונה → `/`.
- [ ] **Step 5: commit** `feat(app): password auth with signed cookie and proxy`.

---

### Task 6: App shell — sidebar, header, דשבורד

**Files:** Create `src/app/(app)/layout.tsx`, `src/app/(app)/page.tsx`, `src/components/shell/sidebar.tsx`, `nav-link.tsx`, `header.tsx`, `src/components/status-led.tsx`, `money.tsx`, `ledger-strip.tsx`, `empty-state.tsx`, `src/app/loading.tsx`, `error.tsx`, `not-found.tsx`; Move login route group as is.

**Interfaces (Produces):**
- `<StatusLed table status />` — נקודה 8px עם glow חלש + תווית מ-`statusMeta`.
- `<Money value />` — `<span className="num">` עם `ils(value)`.
- `<LedgerStrip items={[{label, value, hint?, hero?}]} />` — רצועה אופקית, פריט hero גדול (28px/600), שאר הפריטים 14px/500, מפרידים דקים, "קו תלישה" מנוקד מתחת.
- `<EmptyState title hint action? />`.
- Sidebar: ניווט טקסטואלי עם ספירות (חשבוניות פתוחות, לידים New, משימות open) — נטענות ב-layout דרך `Promise.all`.

- [ ] **Step 1: Intent checkpoint** (חובה לפי interface-design, נכתב בתחילת הקובץ כהערה קצרה):
```
Intent: בעל חנות אלקטרוניקה, פותח בבוקר עם קפה, רוצה לדעת "מה נכנס, מה פתוח, מה דורש ממני פעולה". תחושה: פנקס מסודר על שולחן — חם, שקט, מדויק.
Hierarchy: הכנסות החודש הן הגיבור (28/600). כל השאר 14px, הבדל דרך weight+opacity.
Palette: paper/ink/inkblue; LED רק לסטטוס. Depth: borders-only (rule, rule-strong), צל אחד עדין רק לכרטיס login/dialog.
Surfaces: paper (קנבס) → paper-2 (כרטיס/טבלה) → paper-3 (שקע: input, שורה מודגשת). Typography: Plex Sans Hebrew 14 (ratio 1.25: 11/14/18/22/28), Frank Ruhl Libre לכותרות עמוד.
Spacing: 8px; צפיפות workbench: padding 12/16, שורת טבלה 40px, sidebar 232px.
```

- [ ] **Step 2: components**

```tsx
// status-led.tsx
import { statusMeta } from '@/lib/status';
import type { TableName } from '@/lib/types';
const LED = { green: 'bg-led-green shadow-[0_0_6px_var(--led-green)]', amber: 'bg-led-amber shadow-[0_0_6px_var(--led-amber)]', red: 'bg-led-red shadow-[0_0_6px_var(--led-red)]', off: 'bg-ink-3/40' } as const;
export function StatusLed({ table, status }: { table: TableName; status?: string }) {
  const m = statusMeta(table, status);
  return <span className="inline-flex items-center gap-2 text-ink-2"><span aria-hidden className={`size-2 rounded-full ${LED[m.led]}`} />{m.label}</span>;
}
```
```tsx
// money.tsx
import { ils } from '@/lib/format';
export const Money = ({ value, className = '' }: { value: number; className?: string }) => <span className={`num ${className}`}>{ils(value)}</span>;
```
```tsx
// ledger-strip.tsx
export type LedgerItem = { label: string; value: React.ReactNode; hint?: string; hero?: boolean };
export function LedgerStrip({ items }: { items: LedgerItem[] }) {
  return (
    <section aria-label="סיכום" className="bg-paper-2 border border-rule rounded-lg">
      <div className="flex flex-wrap divide-x divide-x-reverse divide-rule">
        {items.map((it) => (
          <div key={it.label} className={`px-6 py-5 ${it.hero ? 'min-w-64' : 'min-w-40'}`}>
            <div className="text-[11px] font-medium tracking-wide text-ink-3">{it.label}</div>
            <div className={`num mt-1 ${it.hero ? 'text-[28px] leading-none font-semibold text-ink' : 'text-lg font-medium text-ink'}`}>{it.value}</div>
            {it.hint && <div className="mt-1 text-xs text-ink-3">{it.hint}</div>}
          </div>
        ))}
      </div>
      <div aria-hidden className="border-t border-dashed border-rule-strong mx-3" />
    </section>
  );
}
```
```tsx
// empty-state.tsx
export function EmptyState({ title, hint, action }: { title: string; hint?: string; action?: React.ReactNode }) {
  return <div className="text-center py-16 text-ink-2"><p className="font-medium text-ink">{title}</p>{hint && <p className="mt-1 text-sm">{hint}</p>}{action && <div className="mt-4">{action}</div>}</div>;
}
```
```tsx
// shell/nav-link.tsx
'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
export function NavLink({ href, label, count }: { href: string; label: string; count?: number }) {
  const active = usePathname() === href;
  return (
    <Link href={href} aria-current={active ? 'page' : undefined}
      className={`flex items-center justify-between rounded-md px-3 h-9 text-sm transition-colors ${active ? 'bg-inkblue-soft text-inkblue font-medium' : 'text-ink-2 hover:bg-paper-3 hover:text-ink'}`}>
      <span>{label}</span>
      {count ? <span className="num text-xs text-ink-3">{count}</span> : null}
    </Link>
  );
}
```
```tsx
// shell/sidebar.tsx (server)
import { NavLink } from './nav-link';
import { logout } from '@/app/(auth)/login/actions';
export function Sidebar({ counts }: { counts: { invoices: number; leads: number; tasks: number } }) {
  return (
    <aside className="w-[232px] shrink-0 border-e border-rule flex flex-col p-4 gap-6 min-h-dvh">
      <div><div className="text-[11px] font-medium tracking-wide text-ink-3">AI-ERP</div><div className="font-display text-lg font-bold leading-tight">איי.איי אלקטרוניקה</div></div>
      <nav className="flex flex-col gap-1" aria-label="ראשי">
        <NavLink href="/" label="דשבורד" />
        <NavLink href="/invoices" label="חשבוניות" count={counts.invoices} />
        <NavLink href="/leads" label="לידים" count={counts.leads} />
        <NavLink href="/customers" label="לקוחות" />
        <NavLink href="/products" label="מוצרים" />
        <NavLink href="/tasks" label="משימות" count={counts.tasks} />
      </nav>
      <form action={logout} className="mt-auto"><button className="text-xs text-ink-3 hover:text-ink px-3 h-8">יציאה</button></form>
    </aside>
  );
}
```
```tsx
// shell/header.tsx
import { ChatSheet } from '@/components/chat/chat-sheet';
export function Header({ title, actions }: { title: string; actions?: React.ReactNode }) {
  return (
    <header className="flex items-end justify-between gap-4 pb-6">
      <h1 className="text-[28px] font-bold leading-none">{title}</h1>
      <div className="flex items-center gap-2">{actions}<ChatSheet /></div>
    </header>
  );
}
```
(`ChatSheet` נבנה ב-Task 12; עד אז מייצאים stub `export function ChatSheet(){return null}` ב-`components/chat/chat-sheet.tsx`.)

- [ ] **Step 3: (app)/layout.tsx + page.tsx**

```tsx
// (app)/layout.tsx
import { list } from '@/lib/airtable';
import type { InvoiceFields, LeadFields, TaskFields } from '@/lib/types';
import { Sidebar } from '@/components/shell/sidebar';
export const dynamic = 'force-dynamic';
export default async function AppLayout({ children }: { children: React.ReactNode }) {
  const [inv, leads, tasks] = await Promise.all([
    list<InvoiceFields>('Invoices', { filter: "AND({Status}!='paid', {Status}!='error')" }),
    list<LeadFields>('Leads', { filter: "{Status}='New'" }),
    list<TaskFields>('Tasks', { filter: "{Status}!='done'" }),
  ]);
  return (
    <div className="flex min-h-dvh">
      <Sidebar counts={{ invoices: inv.length, leads: leads.length, tasks: tasks.length }} />
      <main className="flex-1 min-w-0 p-8 max-w-[1200px]">{children}</main>
    </div>
  );
}
```
```tsx
// (app)/page.tsx — דשבורד
import Link from 'next/link';
import { list } from '@/lib/airtable';
import { monthKey, dateIL } from '@/lib/format';
import type { InvoiceFields, LeadFields, TaskFields } from '@/lib/types';
import { Header } from '@/components/shell/header';
import { LedgerStrip } from '@/components/ledger-strip';
import { Money } from '@/components/money';
import { StatusLed } from '@/components/status-led';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
export const dynamic = 'force-dynamic';

export default async function Dashboard() {
  const [invoices, leads, tasks] = await Promise.all([
    list<InvoiceFields>('Invoices', { sort: [{ field: 'Created', direction: 'desc' }] }),
    list<LeadFields>('Leads', { sort: [{ field: 'Created', direction: 'desc' }] }),
    list<TaskFields>('Tasks', { filter: "{Status}!='done'" }),
  ]);
  const now = monthKey(new Date().toISOString());
  const valid = invoices.filter((i) => i.fields.Status !== 'error');
  const monthRevenue = valid.filter((i) => monthKey(i.fields.Created) === now).reduce((s, i) => s + (i.fields.Total ?? 0), 0);
  const open = valid.filter((i) => i.fields.Status !== 'paid');
  const openSum = open.reduce((s, i) => s + (i.fields.Total ?? 0), 0);
  const leadCounts = leads.reduce<Record<string, number>>((m, l) => ((m[l.fields.Status ?? 'New'] = (m[l.fields.Status ?? 'New'] ?? 0) + 1), m), {});
  return (
    <>
      <Header title="דשבורד" />
      <LedgerStrip items={[
        { label: 'הכנסות החודש', value: <Money value={monthRevenue} />, hint: `${valid.filter((i) => monthKey(i.fields.Created) === now).length} חשבוניות`, hero: true },
        { label: 'חשבוניות פתוחות', value: open.length, hint: <Money value={openSum} /> as unknown as string },
        { label: 'לידים חדשים', value: leadCounts.New ?? 0, hint: `${leadCounts.Contacted ?? 0} נשלח מייל · ${leadCounts.Qualified ?? 0} ענו` },
        { label: 'משימות להיום', value: tasks.length },
      ]} />
      <div className="grid md:grid-cols-2 gap-6 mt-8">
        <section>
          <div className="flex items-baseline justify-between mb-3"><h2 className="text-lg font-bold">חשבוניות אחרונות</h2><Link href="/invoices" className="text-sm text-inkblue">כל החשבוניות</Link></div>
          <div className="bg-paper-2 border border-rule rounded-lg overflow-hidden">
            <Table><TableHeader><TableRow><TableHead>מספר</TableHead><TableHead>לקוח</TableHead><TableHead>סה"כ</TableHead><TableHead>סטטוס</TableHead></TableRow></TableHeader>
              <TableBody>{invoices.slice(0, 6).map((i) => (
                <TableRow key={i.id}><TableCell className="num font-medium">{i.fields.InvoiceNumber ?? '—'}</TableCell><TableCell>{i.fields.CustomerId}</TableCell><TableCell><Money value={i.fields.Total ?? 0} /></TableCell><TableCell><StatusLed table="Invoices" status={i.fields.Status} /></TableCell></TableRow>
              ))}</TableBody></Table>
          </div>
        </section>
        <section>
          <div className="flex items-baseline justify-between mb-3"><h2 className="text-lg font-bold">לידים אחרונים</h2><Link href="/leads" className="text-sm text-inkblue">כל הלידים</Link></div>
          <div className="bg-paper-2 border border-rule rounded-lg overflow-hidden">
            <Table><TableHeader><TableRow><TableHead>שם</TableHead><TableHead>חברה</TableHead><TableHead>נוצר</TableHead><TableHead>סטטוס</TableHead></TableRow></TableHeader>
              <TableBody>{leads.slice(0, 6).map((l) => (
                <TableRow key={l.id}><TableCell className="font-medium">{l.fields.Name}</TableCell><TableCell className="text-ink-2">{l.fields.Company ?? '—'}</TableCell><TableCell className="num text-ink-2">{dateIL(l.fields.Created)}</TableCell><TableCell><StatusLed table="Leads" status={l.fields.Status} /></TableCell></TableRow>
              ))}</TableBody></Table>
          </div>
        </section>
      </div>
    </>
  );
}
```
`hint` ב-LedgerStrip הופך ל-`React.ReactNode` (לא string) כדי שלא יידרש cast — מתקנים את הטיפוס ב-`ledger-strip.tsx`.

`loading.tsx`: `Skeleton` של רצועה + שתי טבלאות. `error.tsx` (client): כותרת "משהו השתבש", `error.message`, כפתור "נסה שוב" (`reset()`). `not-found.tsx`: "העמוד לא נמצא" + קישור לדשבורד.

- [ ] **Step 4: בדיקה** — `pnpm dev`, כניסה, דשבורד מציג 1,180.00 ₪ הכנסות (INV-0001), 1 חשבונית פתוחה, לידים, משימה 1. `pnpm lint && pnpm build` ירוקים.
- [ ] **Step 5: commit** `feat(app): shell, sidebar with counts, ledger dashboard`.

---

### Task 7: חשבוניות — טבלה, סינון, יצירה, סימון שולם

**Files:** Create `src/app/(app)/invoices/page.tsx`, `actions.ts`, `new-invoice-dialog.tsx`, `src/components/forms/submit-button.tsx`, `field-error.tsx`; Test `src/app/(app)/invoices/actions.test.ts`

**Interfaces (Produces):** `createInvoice(prev, formData)` → `{ error?: string; ok?: boolean }` (zod: CustomerId required, Amount > 0) → `erpCreate('Invoices', {CustomerId, Amount, Status:'new'})` → `revalidatePath('/invoices')`, `revalidatePath('/')`. `markPaid(id)` → `erpUpdate('Invoices', id, {Status:'paid'})`. searchParams: `?q=` (חיפוש במספר/לקוח), `?status=`.

- [ ] **Step 1: test של הולידציה** (הפעולה מפוצלת: `parseInvoiceForm(formData)` טהור ונבדק; `createInvoice` קורא לו)

```ts
import { describe, it, expect } from 'vitest';
import { parseInvoiceForm } from './parse';
const fd = (o: Record<string, string>) => { const f = new FormData(); Object.entries(o).forEach(([k, v]) => f.set(k, v)); return f; };
describe('parseInvoiceForm', () => {
  it('accepts a valid form and coerces amount', () => {
    expect(parseInvoiceForm(fd({ CustomerId: 'CUST-0001', Amount: '1000' }))).toEqual({ ok: true, data: { CustomerId: 'CUST-0001', Amount: 1000 } });
  });
  it('rejects missing customer and non-positive amount with hebrew messages', () => {
    const r = parseInvoiceForm(fd({ CustomerId: '', Amount: '0' }));
    expect(r.ok).toBe(false);
    if (!r.ok) { expect(r.errors.CustomerId).toMatch(/לקוח/); expect(r.errors.Amount).toMatch(/גדול מ-0/); }
  });
});
```

- [ ] **Step 2: FAIL**, **Step 3: implement**

```ts
// invoices/parse.ts
import { z } from 'zod';
const schema = z.object({ CustomerId: z.string().trim().min(1, 'יש לבחור לקוח'), Amount: z.coerce.number().positive('הסכום חייב להיות גדול מ-0') });
export type InvoiceInput = z.infer<typeof schema>;
export function parseInvoiceForm(fd: FormData): { ok: true; data: InvoiceInput } | { ok: false; errors: Record<string, string> } {
  const r = schema.safeParse({ CustomerId: fd.get('CustomerId'), Amount: fd.get('Amount') });
  if (r.success) return { ok: true, data: r.data };
  const errors: Record<string, string> = {};
  for (const i of r.error.issues) errors[String(i.path[0])] ??= i.message;
  return { ok: false, errors };
}
```
```ts
// invoices/actions.ts
'use server';
import { revalidatePath } from 'next/cache';
import { erpCreate, erpUpdate, ErpError } from '@/lib/n8n';
import type { InvoiceFields } from '@/lib/types';
import { parseInvoiceForm } from './parse';
export type FormState = { ok?: boolean; error?: string; errors?: Record<string, string> };
export async function createInvoice(_p: FormState | undefined, fd: FormData): Promise<FormState> {
  const parsed = parseInvoiceForm(fd);
  if (!parsed.ok) return { errors: parsed.errors };
  try { await erpCreate<InvoiceFields>('Invoices', { ...parsed.data, Status: 'new' }); }
  catch (e) { return { error: e instanceof ErpError ? e.message : 'שגיאה ביצירת החשבונית' }; }
  revalidatePath('/invoices'); revalidatePath('/');
  return { ok: true };
}
export async function markPaid(id: string) {
  await erpUpdate<InvoiceFields>('Invoices', id, { Status: 'paid' });
  revalidatePath('/invoices'); revalidatePath('/');
}
```
```tsx
// components/forms/submit-button.tsx
'use client';
import { useFormStatus } from 'react-dom';
import { Button } from '@/components/ui/button';
export function SubmitButton({ children, pendingText = 'שומר…' }: { children: React.ReactNode; pendingText?: string }) {
  const { pending } = useFormStatus();
  return <Button type="submit" disabled={pending}>{pending ? pendingText : children}</Button>;
}
```
```tsx
// components/forms/field-error.tsx
export const FieldError = ({ msg }: { msg?: string }) => (msg ? <p role="alert" className="text-sm text-led-red mt-1">{msg}</p> : null);
```
```tsx
// invoices/new-invoice-dialog.tsx
'use client';
import { useActionState, useEffect, useState } from 'react';
import { toast } from 'sonner';
import { createInvoice, type FormState } from './actions';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/label';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { SubmitButton } from '@/components/forms/submit-button';
import { FieldError } from '@/components/forms/field-error';
export function NewInvoiceDialog({ customers }: { customers: { id: string; label: string }[] }) {
  const [open, setOpen] = useState(false);
  const [state, action] = useActionState<FormState | undefined, FormData>(createInvoice, undefined);
  useEffect(() => { if (state?.ok) { toast.success('החשבונית נוצרה. האימות והמע"מ יחושבו תוך דקה.'); setOpen(false); } if (state?.error) toast.error(state.error); }, [state]);
  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild><Button>חשבונית חדשה</Button></DialogTrigger>
      <DialogContent dir="rtl">
        <DialogHeader><DialogTitle>חשבונית חדשה</DialogTitle></DialogHeader>
        <form action={action} className="space-y-4">
          <div><Label>לקוח</Label>
            <Select name="CustomerId"><SelectTrigger><SelectValue placeholder="בחר לקוח" /></SelectTrigger>
              <SelectContent>{customers.map((c) => <SelectItem key={c.id} value={c.id}>{c.label}</SelectItem>)}</SelectContent></Select>
            <FieldError msg={state?.errors?.CustomerId} /></div>
          <div><Label htmlFor="Amount">סכום לפני מע"מ (₪)</Label><Input id="Amount" name="Amount" type="number" step="0.01" min="0.01" inputMode="decimal" className="num" required /><FieldError msg={state?.errors?.Amount} /></div>
          <p className="text-xs text-ink-3">המע"מ (18%) והמספר הרץ מחושבים אוטומטית על ידי n8n.</p>
          <div className="flex justify-end gap-2"><Button type="button" variant="ghost" onClick={() => setOpen(false)}>ביטול</Button><SubmitButton>יצירה</SubmitButton></div>
        </form>
      </DialogContent>
    </Dialog>
  );
}
```
(תיקון ייבוא: `Input` מ-`@/components/ui/input`.)

```tsx
// invoices/page.tsx
import Link from 'next/link';
import { list, escapeFormula } from '@/lib/airtable';
import { dateIL } from '@/lib/format';
import { INVOICE_STATUSES, type CustomerFields, type InvoiceFields } from '@/lib/types';
import { statusMeta } from '@/lib/status';
import { Header } from '@/components/shell/header';
import { Money } from '@/components/money';
import { StatusLed } from '@/components/status-led';
import { EmptyState } from '@/components/empty-state';
import { NewInvoiceDialog } from './new-invoice-dialog';
import { markPaid } from './actions';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
export const dynamic = 'force-dynamic';
type SP = { q?: string; status?: string };
export default async function InvoicesPage({ searchParams }: { searchParams: Promise<SP> }) {
  const { q = '', status = '' } = await searchParams;
  const filters: string[] = [];
  if (status) filters.push(`{Status}='${escapeFormula(status)}'`);
  if (q) filters.push(`OR(FIND(LOWER('${escapeFormula(q.toLowerCase())}'), LOWER({InvoiceNumber})), FIND(LOWER('${escapeFormula(q.toLowerCase())}'), LOWER({CustomerId})))`);
  const [invoices, customers] = await Promise.all([
    list<InvoiceFields>('Invoices', { filter: filters.length ? `AND(${filters.join(',')})` : undefined, sort: [{ field: 'Created', direction: 'desc' }] }),
    list<CustomerFields>('Customers', { sort: [{ field: 'Name' }] }),
  ]);
  const byId = new Map(customers.map((c) => [c.fields.CustomerId, c.fields.Name]));
  return (
    <>
      <Header title="חשבוניות" actions={<NewInvoiceDialog customers={customers.map((c) => ({ id: c.fields.CustomerId, label: `${c.fields.Name} · ${c.fields.CustomerId}` }))} />} />
      <form className="flex flex-wrap gap-2 mb-4" role="search">
        <Input name="q" defaultValue={q} placeholder="חיפוש לפי מספר או לקוח" className="max-w-xs" />
        <select name="status" defaultValue={status} className="h-9 rounded-md border border-input bg-paper-3 px-3 text-sm">
          <option value="">כל הסטטוסים</option>{INVOICE_STATUSES.map((s) => <option key={s} value={s}>{statusMeta('Invoices', s).label}</option>)}
        </select>
        <Button type="submit" variant="secondary">סינון</Button>{(q || status) && <Link href="/invoices" className="text-sm text-inkblue self-center">נקה</Link>}
      </form>
      <div className="bg-paper-2 border border-rule rounded-lg overflow-hidden">
        {invoices.length === 0 ? <EmptyState title="אין חשבוניות" hint={q || status ? 'נסה סינון אחר' : 'צור את החשבונית הראשונה'} /> : (
          <Table>
            <TableHeader><TableRow><TableHead>מספר</TableHead><TableHead>לקוח</TableHead><TableHead>תאריך</TableHead><TableHead>לפני מע"מ</TableHead><TableHead>מע"מ</TableHead><TableHead>סה"כ</TableHead><TableHead>סטטוס</TableHead><TableHead>מסמך</TableHead><TableHead /></TableRow></TableHeader>
            <TableBody>{invoices.map((i) => (
              <TableRow key={i.id}>
                <TableCell className="num font-medium">{i.fields.InvoiceNumber ?? '—'}</TableCell>
                <TableCell>{byId.get(i.fields.CustomerId) ?? i.fields.CustomerId}<div className="text-xs text-ink-3 num">{i.fields.CustomerId}</div></TableCell>
                <TableCell className="num text-ink-2">{dateIL(i.fields.Created)}</TableCell>
                <TableCell><Money value={i.fields.Amount ?? 0} /></TableCell>
                <TableCell className="text-ink-2"><Money value={i.fields.VatAmount ?? 0} /></TableCell>
                <TableCell className="font-medium"><Money value={i.fields.Total ?? 0} /></TableCell>
                <TableCell><StatusLed table="Invoices" status={i.fields.Status} /></TableCell>
                <TableCell>{i.fields.PdfUrl ? <a href={i.fields.PdfUrl} target="_blank" rel="noreferrer" className="text-inkblue text-sm">PDF</a> : <span className="text-ink-3 text-sm">—</span>}</TableCell>
                <TableCell>{i.fields.Status === 'generated' && <form action={markPaid.bind(null, i.id)}><Button size="sm" variant="outline">סמן שולם</Button></form>}</TableCell>
              </TableRow>))}</TableBody>
          </Table>)}
      </div>
    </>
  );
}
```

- [ ] **Step 4: בדיקה ידנית** — יצירת חשבונית ל-דוד לוי 500 ₪ → toast → מופיעה כ"חדש"; אחרי ~2 דקות רענון: INV-0002, 90/590, "הופק PDF" + קישור; "סמן שולם" → "שולם". סינון לפי סטטוס וחיפוש `INV-0001`. `pnpm test && pnpm build`.
- [ ] **Step 5: commit** `feat(app): invoices list, filters, create dialog, mark paid`.

---

### Task 8: לידים

**Files:** Create `src/app/(app)/leads/page.tsx`, `actions.ts`, `parse.ts`, `parse.test.ts`, `new-lead-dialog.tsx`, `status-select.tsx`

**Interfaces (Produces):** `createLead` (Name required, Email תקין, Company אופציונלי) → `erpCreate('Leads', {...})` (Status נקבע ב-WF2). `setLeadStatus(id, status)` → `erpUpdate`. `runSalesNow()` → `runWebhook('run-sales')` ומחזיר `{sentTo?}`.

- [ ] **Step 1: test** — כמו Task 7: `parseLeadForm`: שם ריק → "יש להזין שם", אימייל לא תקין → "אימייל לא תקין", חברה ריקה מותרת.
- [ ] **Step 2–3: FAIL → implement** — `parse.ts` עם `z.object({ Name: z.string().trim().min(1,'יש להזין שם'), Email: z.string().trim().email('אימייל לא תקין'), Company: z.string().trim().optional().transform(v => v || undefined) })`. `actions.ts`: `createLead`, `setLeadStatus`, `runSalesNow` (מחזיר `{ ok: true, sentTo }` או `{ error }`; toast בצד ה-client).
- [ ] **Step 4: UI** — `page.tsx`: סינון `?status=` (LEAD_STATUSES), טבלה: שם, חברה, אימייל, נוצר, סטטוס (`<StatusSelect id status/>` — client, `<select>` עם `onChange` שקורא ל-`setLeadStatus` דרך `useTransition`, toast), כפתורים בכותרת: `NewLeadDialog` + `RunSalesButton` ("שלח מייל לליד הבא" — client button, `useTransition`, toast עם `sentTo`).
- [ ] **Step 5: בדיקה** — ליד חדש → אחרי דקה New; "שלח מייל לליד הבא" → toast "נשלח ל-…" → Contacted. שינוי סטטוס ידני ל-Dead.
- [ ] **Step 6: commit**.

---

### Task 9: לקוחות

**Files:** `src/app/(app)/customers/page.tsx`, `actions.ts`, `parse.ts`, `parse.test.ts`, `new-customer-dialog.tsx`

**Interfaces:** `createCustomer` — `CustomerId` נוצר בשרת: `CUST-` + `String(count+1).padStart(4,'0')` לפי `list('Customers')` (ידוע: התנגשות אפשרית בשני יוצרים באותה שנייה — מקובל כמו במסמך); Name required, Email אופציונלי תקין, Phone אופציונלי.
- [ ] test → FAIL → implement → UI (טבלה: מזהה, שם, אימייל, טלפון, מספר חשבוניות (join עם Invoices), סה"כ) → בדיקה → commit.

---

### Task 10: מוצרים

**Files:** `src/app/(app)/products/page.tsx`, `actions.ts`, `parse.ts`, `parse.test.ts`, `new-product-dialog.tsx`, `reindex-button.tsx`

**Interfaces:** `createProduct` (Name, Category, Price ≥ 0, Description, InStock boolean מ-checkbox `on`), `toggleStock(id, inStock)`, `reindexProducts()` → `runWebhook('reindex-products')` → `{ products }`.
- UI: סינון `?category=` (רשימת קטגוריות מהנתונים), חיפוש `?q=` בשם, טבלה: שם, קטגוריה, מחיר, במלאי (LED green/off עם כפתור החלפה), תיאור (line-clamp-2, `title` מלא). כותרת: "מוצר חדש" + "רענון מאגר ידע" (toast: "34 מוצרים נטענו לסוכן").
- test → FAIL → implement → בדיקה (34 מוצרים, 13 קטגוריות) → commit.

---

### Task 11: משימות

**Files:** `src/app/(app)/tasks/page.tsx`, `actions.ts`

**Interfaces:** `addTask(prev, fd)` (Title required) → `erpCreate('Tasks', {Title, Status:'open'})`; `toggleTask(id, done)` → `erpUpdate('Tasks', id, {Status})`.
- UI: שורת הוספה בראש (`<form>` inline: Input + "הוסף"), רשימה: פתוחות למעלה, בוצעו מטה ב-ink-3 עם line-through, כל שורה checkbox נטיבי (`<input type="checkbox">` בתוך form עם `formAction`), 44px גובה.
- בדיקה: הוספה, סימון, ביטול סימון; הספירה ב-sidebar מתעדכנת. commit.

---

### Task 12: צ'אט עם סוכן המנהל

**Files:** `src/app/(app)/chat/actions.ts`, `src/components/chat/chat-sheet.tsx`, `chat-panel.tsx`

**Interfaces:** `sendChat(message: string): Promise<{ reply: string }>` — sessionId מ-cookie `erp_chat` (uuid, נוצר בפעם הראשונה, 30 יום). `ChatSheet`: כפתור "שאל את המנהל" בכותרת → `Sheet` מצד שמאל (side="left" ב-RTL = הצד הרחוק מהניווט), היסטוריה ב-`useState` (מתאפסת ברענון; זה מקובל — הזיכרון האמיתי בסוכן), שאלות מוכנות: "מה ההכנסות החודש?", "כמה חשבוניות לא שולמו?", "מה מצב הלידים?". הודעות: שלי ב-inkblue-soft, של הסוכן ב-paper-3, `whitespace-pre-wrap`. מצב שולח: skeleton של 3 נקודות. Enter שולח, Shift+Enter שורה.
- בדיקה: שאלה → תשובה בעברית עם 1,180.00 ₪ (או הסכום הנוכחי). שאלת המשך "ומה עם הלידים?" — ההקשר נשמר (memory ב-WF9-core לפי sessionId).
- commit.

---

### Task 13: איכות — Playwright smoke, RTL QA, a11y, ליטוש

**Files:** `app/playwright.config.ts`, `app/e2e/smoke.spec.ts`

- [ ] **Step 1: Playwright** (`pnpm dlx playwright install chromium`), config: `baseURL: 'http://localhost:3000'`, `webServer: { command: 'pnpm dev', port: 3000, reuseExistingServer: true }`, `use: { locale: 'he-IL' }`.

```ts
import { test, expect } from '@playwright/test';
const PASSWORD = process.env.APP_PASSWORD ?? '';
test.beforeEach(async ({ page }) => {
  await page.goto('/'); await expect(page).toHaveURL(/\/login/);
  await page.getByLabel('סיסמה').fill(PASSWORD); await page.getByRole('button', { name: 'כניסה' }).click();
  await expect(page.getByRole('heading', { name: 'דשבורד' })).toBeVisible();
});
test('dashboard shows ledger strip and rtl direction', async ({ page }) => {
  await expect(page.locator('html')).toHaveAttribute('dir', 'rtl');
  await expect(page.getByText('הכנסות החודש')).toBeVisible();
  await expect(page.getByText(/₪/).first()).toBeVisible();
});
test('navigation counts and pages render', async ({ page }) => {
  for (const [name, heading] of [['חשבוניות', 'חשבוניות'], ['לידים', 'לידים'], ['לקוחות', 'לקוחות'], ['מוצרים', 'מוצרים'], ['משימות', 'משימות']] as const) {
    await page.getByRole('link', { name: new RegExp(name) }).click();
    await expect(page.getByRole('heading', { name: heading, level: 1 })).toBeVisible();
  }
});
test('creates a task and toggles it', async ({ page }) => {
  await page.goto('/tasks');
  const title = `בדיקה ${Date.now()}`;
  await page.getByPlaceholder('משימה חדשה').fill(title); await page.getByRole('button', { name: 'הוסף' }).click();
  await expect(page.getByText(title)).toBeVisible();
  await page.getByRole('checkbox', { name: title }).check();
  await expect(page.getByText(title)).toHaveCSS('text-decoration-line', 'line-through');
});
test('chat answers in hebrew', async ({ page }) => {
  await page.getByRole('button', { name: 'שאל את המנהל' }).click();
  await page.getByRole('button', { name: 'מה ההכנסות החודש?' }).click();
  await expect(page.getByText(/₪/).last()).toBeVisible({ timeout: 30_000 });
});
```
Run: `APP_PASSWORD=... pnpm e2e` (הסטודנט מריץ עם הסיסמה שלו; או קוראים מ-`.env.local` ב-config עם `dotenv`). Expected: 4 passed.

- [ ] **Step 2: rtl-qa agent** — מריצים את ה-agent `rtl-qa` על `http://localhost:3000` (אחרי login ידני, או עם cookie): שברי RTL, נייד 390px, קונסול. מתקנים ממצאים.
- [ ] **Step 3: design-review** — `/interface-design:design-review` על הדשבורד והחשבוניות; מתקנים: hierarchy, states (hover/focus/empty/loading), concentric radius ב-dialog, tabular-nums בכל מספר.
- [ ] **Step 4: a11y מהיר** — `chrome-devtools-mcp:a11y-debugging`: labels לכל input, focus ring נראה (`ring-inkblue`), contrast ink-3 על paper ≥ 4.5 (אם לא — מכהים `--ink-3` ל-`#7a8087`).
- [ ] **Step 5: lint, typecheck, test, build ירוקים** → commit `test(app): playwright smoke; rtl/a11y fixes`.

---

### Task 14: פריסה ל-Vercel + runbook + דמו

- [ ] **Step 1: Vercel** — `vercel:deploy` skill (מה-`app/`): `vercel link` (פרויקט חדש `ai-erp`), env vars ל-Production דרך `vercel env add` (הסטודנט מקליד ערכים בטרמינל שלו, לא בצ'אט): `AIRTABLE_PAT`, `AIRTABLE_BASE_ID`, `N8N_WEBHOOK_URL`, `N8N_WEBHOOK_SECRET`, `APP_PASSWORD`, `AUTH_SECRET`. `vercel --prod`.
- [ ] **Step 2: אימות בפרודקשן** — URL → login → דשבורד → יצירת חשבונית → תוך 2 דקות PDF. Playwright smoke מול ה-URL: `PLAYWRIGHT_BASE_URL=https://... pnpm e2e`.
- [ ] **Step 3: runbook** — סעיף "8. האפליקציה": env, `pnpm dev`, `pnpm test`, `pnpm e2e`, deploy, ומה קורה כשהמק כבוי (האפליקציה עולה, קריאה מ-Airtable עובדת, כתיבה/צ'אט נכשלים עם toast — ה-n8n מקומי).
- [ ] **Step 4: docs/demo.md** — סקריפט הדמו מקצה לקצה (10 צעדים, 5 דקות): login → דשבורד → חשבונית חדשה → PDF → "סמן שולם" → ליד חדש → "שלח מייל" → תשובה → Qualified → צ'אט "מה ההכנסות?" → בוט שירות בטלגרם.
- [ ] **Step 5: todo + commit** `feat(app): production deploy; runbook and demo script; plan 3 complete`.

---

## בדיקה עצמית מול הספק

- סעיף 7 מסכים: דשבורד T6, חשבוניות T7, לידים T8, לקוחות T9, מוצרים T10, משימות T11, צ'אט T12. ✔
- קריאה בצד השרת בלבד עם PAT ב-env: T4 (`server-only`). ✔ כתיבה דרך WF13: T3 + T7–T12. ✔
- אבטחה משתמש יחיד, cookie חתום, proxy: T5. ✔
- עברית/RTL/₪/dd/MM/yyyy: T1, T2, T13. ✔
- טיפול בשגיאות: `ErpError` → toast; `error.tsx`; WF13 400 → הודעה. ✔
- בדיקות: Vitest (T1–T5, T7–T10), Playwright (T13). ✔
- פריסה Vercel: T14. ✔
- הרחבה נדרשת בצד n8n (update action) מתועדת ב-T3 ומיוצאת ל-`n8n/workflows/exported/`. ✔
- לא נבנה בכוונה: Orders/Suppliers, הרשאות, dark mode, cache. ✔
