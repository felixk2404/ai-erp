# Brand film (HyperFrames)

Script: ../docs/superpowers/specs/2026-09-02-brand-film-script.md · Plan: ../docs/superpowers/plans/2026-09-03-08-brand-film.md

```bash
npx hyperframes check
npx hyperframes preview
npx hyperframes render --quality high --output out/ai-electronics-erp-film.mp4
```

Captures (`captures/`) are produced by `node capture/capture.mjs` (Playwright, needs a one-time login with `--login`) and by Felix (Telegram/Gmail screen recordings, see plan Task 0). Large recordings are not committed.
